import { createContext } from "react";

const NoteContext = createContext(); //NoteContext will contain all states related to notes which can be later used by any components

export default NoteContext;
